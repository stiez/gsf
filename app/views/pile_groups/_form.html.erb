<%= form_for(@pile_group) do |f| %>
  <% if @pile_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pile_group.errors.count, "error") %> prohibited this pile_group from being saved:</h2>

      <ul>
      <% @pile_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Name %><br />
    <%= f.text_field :Name %>
  </div>
  <p><%= collection_select(:pile_group, :station_id, @corpStations.sort{ |first, second| first[1] <=> second[1]}, :first, :last) %></p>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
